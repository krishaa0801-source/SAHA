node_modules/
auth-dist/
.env
.env.local
.DS_Store

# Pre-Cloudinary leftovers: product/review images now live on Cloudinary
# (server/services/imageProcessor.js), never written here anymore. Left
# ignored as a safety net in case anything still lands here locally.
public/uploads/
